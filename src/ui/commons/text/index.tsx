import styled from "@emotion/native";
import { Text as RNText } from "react-native";

export const Text = styled(RNText)`
  font-family: "Outfit";
  font-weight: 400;
  margin: 0;
`;

export const TextMedium = styled(RNText)`
  font-family: "Outfit";
  font-weight: 500;
  margin: 0;
`;

export const TextSemiBold = styled(RNText)`
  font-family: "Outfit";
  font-weight: 700;
  margin: 0;
`;

export const TextBlack = styled(RNText)`
  font-family: "Outfit";
  font-weight: 900;
  margin: 0;
`;
